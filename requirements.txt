# Flyby AI — backend dependencies
#
# Install with:  pip install -r requirements.txt

# Core web framework
Flask
Flask-Cors
Flask-Login
Flask-SQLAlchemy
Flask-Migrate
SQLAlchemy
Werkzeug

# Auth tokens
PyJWT

# Configuration
python-dotenv

# Security hardening (rate limiting, input sanitization) — see utils/security.py
Flask-Limiter
bleach

# Outbound calls: SMS delivery for two-step verification, voice transcription
requests

# Production database driver (SQLite is used in development, no driver needed)
pymysql

# Production WSGI server — what the Docker image runs (the Flask dev server in
# main.py is for localhost only).
gunicorn
